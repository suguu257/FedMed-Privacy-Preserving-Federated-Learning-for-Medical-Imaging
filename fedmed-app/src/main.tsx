import React from 'react'
import ReactDOM from 'react-dom/client'
import FedMedApp from './fedmed_app'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FedMedApp />
  </React.StrictMode>,
)